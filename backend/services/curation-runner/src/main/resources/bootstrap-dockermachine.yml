spring:
  rabbitmq:
    host: "${DOCKER_MACHINE_IP}"
    port: 5673
  cloud:
    config:
      uri:  http://${DOCKER_MACHINE_IP}:1111

mona:
  rest:
    server:
      host: "${DOCKER_MACHINE_IP}"
      port: 8080