eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: "http://${DOCKER_MACHINE_IP}:8761/eureka/"
spring:
  rabbitmq:
    host: "${DOCKER_MACHINE_IP}"
    port: 5673

mona:
  rest:
    server:
      token: