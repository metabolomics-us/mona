spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/metabolomics-us/mona-config
          clone-on-start: true

server:
  port: 1111
