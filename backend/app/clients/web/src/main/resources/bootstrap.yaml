#basic configuration how to connect to the configuration server
spring:
  application:
    name: angularjs-service
  cloud:
    config:
      uri:  http://localhost:1111

#basic information who we are
info:
  component: MoNA AngularJS Services

logging:
  level:
    ROOT: INFO

#on which port do we run
server:
  port: 9999
