/* TODO: Write Test, omiting for now, testing services first
'use strict'

describe('Controller: Authentication Controller', function() {
  beforeEach(module('moaClientApp'));

  var scope, controller;

  beforeEach(inject(function($controller,$rootScope) {
      scope = $rootScope.$new();
      controller = $controller('AuthenticationController', {$scope: scope});
  }));

  //console.log('CONTROLLER ' + moaControllers.AuthenticationController);
  console.log('CONTROLELR ' + moaControllers.AuthenticationController);

});*/
